const { connect } = require('./client');
const { setupInput } = require('./inout');
console.log('Connecting ...');
connect();

const { connect } = require('./client)');
const { setupInput } = require('./input');
console.log('Connecting ...');
connect();


setupInput();